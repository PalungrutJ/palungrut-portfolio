# Project screenshots

Drop **anonymized** project screenshots here (e.g. `qc-inspection.webp`) and
reference them from `src/data/projects.ts` via each project's `image` field.

Never commit confidential data: no internal IPs, server names, real employee
data, production database names, or proprietary source. Blur or replace any
sensitive values with sample data before adding an image.
