# Resume PDF

Place your resume PDF here as:

    palungrut-jankong-resume.pdf

The download button and inline preview read this path from
`src/data/profile.ts` (`profile.resumeUrl`). If you use a different
filename, update that value to match.
